<h2>Edit post</h2>
<p>Posts can be formatted using <a href="https://en.wikipedia.org/wiki/Markdown">Markdown</a>.</p>
{{#has_error}}
<p><strong>Uh oh</strong>. Your post has issues:</p>
<ul class="errors">
{{#errors}}
    <li>{{.}}</li>
{{/errors}}
</ul>
{{/has_error}}
<form action="{{baseurl}}post/edit/{{id}}" method="post">
    <input type="hidden" name="csrf" value="{{csrf}}" />
    <textarea name="content">{{content}}</textarea>
    <input type="submit" value="Edit" />
</form>
<form action="{{baseurl}}post/delete/{{id}}" method="post">
    <input type="hidden" name="csrf" value="{{csrf}}" />
    <input type="submit" value="Delete" />
</form>
{{#can_move}}
<p>You may also move this into a different thread.</p>
<form action="{{baseurl}}post/move/{{id}}" method="post">
    <input type="hidden" name="csrf" value="{{csrf}}" />
    <label for="thread_id">Thread ID (0 for new thread)</label>
    <input type="number" name="thread_id" value="0" />
    <input type="submit" value="Move" />
</form>
{{/can_move}}
